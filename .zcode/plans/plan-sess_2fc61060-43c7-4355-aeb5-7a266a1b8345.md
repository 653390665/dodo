Round 42 修复计划（246-249 · 四个 P1 精准修复）

四个 P1 全部有代码级根因定位，每个都是 S-M 级精准修复，一轮收完。

## 246: 分镜来源徽标不诚实（双端，S-M）
- 后端 production.ts 两处 fallbackContinuity 补 degradation 字段
- 前端 ProductionRunReview 徽标数据源改为 displayRun 优先
- 验证：保底 run 显示 ⚠️、模型 run 显示 AI 分镜、历史一致

## 247: 生产期 critic unknown 自动重试（后端，S）
- ai-production-pipeline.ts:642 unknown 不再直接 break，追加一次 critic 复核
- 验证：mock 首次无证据→重试后 pass；两次均 unknown→正确降级

## 248: 流程步骤静默失败加提示（前端，S）
- PlanningTab.tsx:149 静默 return 加 toast；stepError 样式放大
- 验证：组件测试

## 249: 知识图谱入口提升（前端，S）
- AgentWorkspace 场景图谱卡片加「查看完整图谱」按钮
- 验证：组件测试

## 执行顺序
246 → 247 → 248 → 249，无硬依赖可并行，全部 S-M 级一轮收完。

## 不在本轮
- 245 大项（入口收敛到步骤条）：涉及 E2E 契约重锚，另立
- 顶级导航归并（AI 协作 vs 智能管家）：待归并议题一并议
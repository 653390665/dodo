# Round 34 立项：把审计发现落成 8 份计划 + 215 增补 + 台账登记

按已确认的默认组合，在 `plans/` 下新写 8 份计划文件（沿用仓库模板：Status/Why/Current state（含亲读代码摘录）/Commands/Scope/Steps+Verify/Done/STOP）、修订 215、登记台账并提交推送（延续 Round 31-33 惯例）。基线 commit：`d6c2ed2`。

## 8 份计划文件

**216：start-stream 质量拒绝交接修复**（CORR-01，S）
`production.ts:1006` pipeline 未 await、`:1151` 前缀错配（`DRAFT_QUALITY_REJECTED:` vs 检查的 `GATE_FAILED:`）→ 全失败场景草稿静默丢弃、`:1196` 交接死代码。修法：catch 链补 `DraftQualityRejectionError` 分支并复用外层交接逻辑；用 `__productionTestHooks` 写确定性测试。

**217：@xmldom/xmldom 钉版 + CI 审计门解阻**（DEPS-01/SEC-01，S-M）
`npm audit --omit=dev` 1 high（mammoth→xmldom 0.8.13），build.yml:39 豁免表空 → push main 必挂。修法：overrides 加 `"@xmldom/xmldom": "^0.9"`（模式仿现有 sharp/nanoid/postcss/tar 条目）+ 跑 docx 导入回归；mammoth 不兼容则回退为带到期日的豁免条目并立升级计划。

**218：流收尾 promise 收口**（CORR-03，S）
`production.ts:1118-1183` 终态 promise 无人接住，catch 内 DB 写/refundQuota 抛错 → unhandledRejection → 整进程退出（server.ts:199）。修法：终态 `.catch(logger.error)` + 处理器内 try/catch；补收尾写失败的降级用例。

**219：AGENTS.md 测试命令地图**（DX-01，S）
`npm test` 只跑后端，AGENTS.md Validation 无命令地图。修法：AGENTS.md 加「改 server→npm test；改 src→test:frontend（可带路径）；契约改动→双端」地图 + package.json 增 `test:unit` 聚合脚本。

**220：治理目录投影 memo 化**（PERF-02/03，S-M）
SkillsStudioView 每渲染（含弹窗按键）10+ 次全量 filter、货架 73 卡每卡 O(目录) 扫描。修法：投影收进 useMemo（模板：:597 filteredCuratedSkills）、sanitize-required id 集合模块级预计算为 Set、StyleShelf memo 化；依赖数组纳入 savedSkills。

**221：副本单源化不变式入规范 + 同名导出澄清**（DOCS-01/02，S）
新建 `docs/specs/capability-sanitize.md`（白标泄露风险、两条合法投影路径、守护测试指针），AGENTS.md 不变式节增补第四条；`public-skill-catalog.ts` 的同名 `PROMPT_GOVERNANCE_CATALOG` 重命名为 `PUBLIC_SKILL_GOVERNANCE_CATALOG`（触点约 10 处 import 同步改）。

**222：AgentWorkspace 打字路径性能**（PERF-04，S-M）
主 textarea 每键 setSelection → 1101 行组件重渲 + 每键全文 join 哈希。修法：去掉 selection state，debounce 定时器内直读 `textarea.selectionStart`；保留「无焦点时全量扫描」语义与既有测试基线。

**223：第二梯队打包（8 项 S 级）**
①CI/本地双构建消除（build.yml 已前置 build，playwright webServer 的 build 保留但 test:all 去重或注明）；②两处同步 revokeObjectURL 对齐 download-client 延迟模式；③README 防抖 3 秒→1 秒 + 状态表日期刷新；④.env.example 补 6 个 INKFLOW_* 旋钮小节；⑤db.ts:402 裸文件 fallback 改 404；⑥prepare 失败改显式警告；⑦删 autoprefixer/@axe-core/react；⑧vitest coverage 加 `include: ['src/**']` + 前后端 coverage 目录分离 + 棘轮基线重锚。每项独立 Verify，可分 commit。

## 215 修订

计划文件追加「回归守卫」步骤：新增断言「runtimeStatus==='active' ⇒ template 不以 `[商业定制专属提示词体]` 开头」（现存 33 张先入显式豁免清单锚定），防止修完即回归。

## 台账（plans/README.md）

- 新增 216-223 八行（TODO）+ 215 行备注增补守卫步骤
- 新增「Round 34 说明（2026-09-15）」：来源=improve 四路审计（记录审计方法与覆盖面）
- 新增「审计 backlog（第三梯队，未立项）」小节：16-25 号发现与 3 条 direction 各一行存底，注明后续可按需立项
- 依赖图：216/217/218 独立可并行；221 是后续 #17(白标单源化)/#19(governance 契约测试) 的前置；223 内 ⑧ 独立

## 收尾

- 提交（docs(plans): round 34 立项——审计发现转 216-223）并推送到 origin（延续既有授权惯例）
- 审计的「considered and rejected」记录：SEC-01 并入 217；COVERAGE-02 并入 215；第三梯队 16-25 转 backlog 不算 rejected

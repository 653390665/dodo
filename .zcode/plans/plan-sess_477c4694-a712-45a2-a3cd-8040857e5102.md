# Round 32 计划：遗留问题修复（plans 207-209）

按仓库计划模板（Status/Why/Current state/Commands/Scope/Steps+Verify + Drift check）写 3 份计划文档到 `plans/`，更新 `plans/README.md` 台账（Round 32 行 + 核销 199/205 遗留小项），提交。只写计划文档，不执行实施。

## Plan 207（P1）：完成风暴修复——fact 面板自动补跑限次

**根因**（已亲自核实 `src/components/EditorView.tsx:1245-1262`）：
- `factPanelNeedsGate` 只排除 `ready`/`accepted-risk`，把已评估的 `review-required`/`needs-action` 也视为「需要补跑」
- 自动补跑 effect（依赖 `[factPanelNeedsGate, isCompletingChapter]`）无「每候选只跑一次」标记；成功路径 `:1208-1215` 把候选重新装回 state，`finally :1227` 翻回 in-flight → effect 再触发，形成 ~35ms/轮的串行自激循环（10 秒 283 次）
- 后端 `server/helpers/chapter-completion.ts:154-176` 幂等缓存使风暴「便宜但恒定返回非 ready 门」，循环自我维持

**修复方向**：
1. 触发语义收窄：仅当门未评估（`undefined`/`'drafting'`）时自动补跑；`review-required`/`needs-action` 属已评估，交给面板确认流程
2. 兜底限次：按 `candidateRunId`（或候选身份）的 once-ref 标记，章节/候选变更时重置——即使门被 GET 刷新（`:1186-1204` 整体覆盖）冲回旧值也不再循环

**验证接缝**（只测外部行为）：
- 新增组件级回归测试（仿 `src/tests/chapter-fact-candidate-review.test.tsx` 的 mock 方式）：`review-required` 门 + 候选存在 + complete 返回同门 → `completeChapter` 恰被调 1 次
- 解阻塞现成 E2E：`tests/e2e/unified-creation-new-project.spec.ts:185` `completionCalls toBe(1)` + `unified-creation-imported-project.spec.ts` 转绿

**Out of scope**：服务端限流（后端已有幂等缓存，根因在前端）；plan150/full-browser 缓议项

## Plan 208（P2）：导出菜单 portal 化——脱离编辑器堆叠命中区

**现状**：`src/components/EditorStatusBar.tsx:166-188` 菜单为 `absolute bottom-full z-50` 弹层，向上弹入 `WritingSurface`（`min-h-[70vh]` textarea，`WritingSurface.tsx:313`）所在层叠上下文，命中测试被 textarea 覆盖 → `tests/e2e/core-flow.spec.ts:359-362` 点击脆弱（既有脆弱性，非回归；git 证据：plan199 的修复实为移除根节点 overflow-hidden）

**修复**：菜单改 `createPortal(document.body)` + `fixed` 定位（打开时按触发按钮 rect 锚定）；保留 `role=menu`/`menuitem`、`aria-haspopup`/`aria-expanded`、document 级外点/Escape 关闭。**不新增依赖**（仓库唯一 portal 先例 Radix AlertDialog；无 floating-ui/headlessui，加依赖需审批故不用）

**验证接缝**：
- 新增 `EditorStatusBar` 导出菜单单测（现无单测）：getByRole 穿透 portal 断言开/关/选中触发导出 fetch（仿 `editor-guidance-layout.test.tsx` 渲染方式）
- `core-flow.spec.ts` role 点击不变且转绿

## Plan 209（小项）：弹窗 aria id 硬化 + act() 警告清理

- `PackageConfigDialog.tsx:346/:682` 与 `SkillsStudioView.tsx:2720/:2850/:2855` 的固定字符串 `aria-labelledby`/`aria-describedby` id 改 `useId` 派生（全仓库目前无 useId；弹窗 aria 配对标_id 是本次硬化面，不动全仓库确定性 id 惯例）
- 两处字面量断言改关系断言：`plan158-skills-studio-candidates.test.tsx:275`、`skills-studio-plan158.test.tsx:1590`（`getByRole('dialog', {name})` / within 内解析）
- act() 警告：「启用所选」async onClick（`PackageConfigDialog.tsx:737-755`）await 后的 store 更新包进 `await act(...)` 或改 waitFor settle（对齐 `skills-package-store.test.tsx:182-201` 模式）

## 台账与收尾

- `plans/README.md`：Round 32 新增 207/208/209 行（Priority P1/P2/P3）；199 行补「风暴已由 207 修复、unified-new 转绿」（plan150/full-browser 缓议保留）；205 行遗留小项核销至 209
- 执行顺序 207→208→209，各自独立提交；Drift check 基准 `95aefc3`
- 验证命令：后端 `npm run test:coverage:backend`、前端 vitest 全量、定向 E2E（core-flow、unified-creation ×2）；197/185 维持 BLOCKED 不动

风险提示：207 触碰 EditorView 完成流程（本仓库最敏感区），靠组件回归测试 + 既有三个完成流测试套件 + E2E 兜底。
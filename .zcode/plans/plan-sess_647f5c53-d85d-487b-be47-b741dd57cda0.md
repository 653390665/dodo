## 013 — 能力卡全量普查落地：分类学 + 适合度评分 + 消重（纯展示层）

### 背景（侦察已完成，两轮 Explore + 实跑数据）
- 内部目录 174 条（active 128 上架）+ 精选 16 + 流程 6 + 包 9；22 个流程步骤资产中 **19 个双重身份**（既是流程步骤又是独立卡）；`opening-gold-three` ≡ `plaza-golden-three` 同标题异 id 真重复
- 73 张文风卡 = 31 个系列/孤岛（风华 25、lwl 13、小飞鸡 6…），混入大纲/世界观/角色等非文风功能
- 治理分 30-98 挤在 C 级；题材标签仅 16/127 且英文 slug 与作品中文 tags 不对齐（`inferNovelGovernanceProfile` 文本推断已能产出英文 token，可复用）
- 货架排序 = 目录插入顺序（无排序）；反馈分通道完整但 0 样本不参与排序

### 用户已确认的方向（默认项）
适合度分（展示层计算）｜文本特征化＋离线补数据｜标记＋折叠消重｜纯展示层不动调用链

### 实施步骤

**1. 离线数据补全**（一次性脚本改 `shared/lib/prompt-governance-catalog.ts`）
- 规则映射表：标题/goal 题材词（玄幻/都市/番茄/短篇/脑洞…中英对照）→ `genreTags`；"番茄/老福特" → `platformTags: ['tomato']/'laofo'
- 目标：73 张 optional-style 的 genreTags 覆盖率 12% → ≥90%；数据补全不改任何行为

**2. 分类学纯函数**（`src/lib/capability-shelf.ts` 新文件）
- `getStyleShelfGroups(assets, novel)`：五功能域分组（正文润色与文风/故事生成/大纲与设定/题材模板/拆书与仿写）+ 系列折叠组（前缀 ≥3 张：风华/lwl/小飞鸡/天马）+ 「已含于流程」标记（assetId ∈ 22 个流程步骤 id 或语义重合表）
- `computeCardFitness(asset, novelContext)`：五信号加权——题材命中(30)/平台匹配(20)/阶段匹配(15)/使用反馈(20，0 样本时权重自动重分配给前两项)/治理分归一(15)；返回 `{score, reasons[]}`（"✓玄幻题材""✓番茄平台"）
- 两组表驱动测试

**3. 前端展示**（`SkillsStudioView.tsx` 文风与正文页签）
- 页签内二级分组渲染（组头 + 计数 + 折叠），系列卡默认折叠
- 卡面：「冷启动证据 86」→ 适合度 chip + 匹配原因 chips；治理分移入详情
- 与流程重合卡：「已含于创作流程」徽章 + 默认收进系列组
- 组内排序：题材命中 → fitness → 目录序

**4. 冲突面记录（不在本轮修复）**
- 19 张双重身份卡的运行时双注入风险：标注待验证项写入 013 文档，需实测后另立修复
- `getAssetEnhancementPackage` 陈旧桥接（包 steps 与硬编码映射对不上）同记录

### 验证门
- `npx tsc --noEmit` 0；eslint 改动文件 0；前端 vitest 全绿（841+新增分组/评分/消重用例）
- 73 张卡分组覆盖率 100%（每组有归类，无"未分类"桶，除系列折叠组）

### 边界
- 不动治理分字段本身、不动调用链与后端、不删除任何卡
- 适合度是展示层计算，不写入资产
### 完成标准
- [ ] 分组/折叠/徽章/适合度 chip 在文风与正文页签可见
- [ ] genreTags 覆盖率 ≥90%
- [ ] 全量测试绿
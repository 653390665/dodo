写 Round 31 计划批次（plans/196-206，共 11 个），沿用 Round 30 自包含模板，并更新台账。

## 批次构成（编号/范围/关键锚点/验收门）

1. **196-vite7-upgrade**：Vite 6→7（190 Step3 遗留）。vite ^6.4.3→^7、@vitejs/plugin-react→latest（顺带从 dependencies 挪 devDependencies）、vitest 4 互操作确认。三链路验证：server.ts:128-141 middlewareMode dev、`npm run build`、`npm run test:frontend`。Verify：npm ls vite 7.x + 三链路绿 + E2E 全套。STOP：vitest 4 无法共存且需大版本升级波及 800+ 用例则回退另立。
2. **197-sanitize-filter-unblock**（185 拍板：扩过滤器保特性）：generate-public-catalog.ts:53 `isPublicRuntimeAsset` 目前剔除 45 张 sanitize-required（prompt-governance-catalog.ts 44 + 静态 raw-comp-brand-detector）。为它们生成 `sanitized-<id>` 消毒副本条目（镜像 /api/skills/sanitize 的克隆语义：sanitizeWhiteLabelText 全字段 + runtime-ready/active + sourceType 授权枚举），同时把 tests/public-catalog-freshness.test.ts:56 的本地过滤器副本抽成共享单源；然后 src/lib/capability-governance.ts:321 getOptionalStyleAssets 切换为只消费 sanitized 副本（185 的原始目标）。plan158 的「消毒并启用」45 张断言按新语义更新。STOP：消毒管线输出语义失效（空文案/关键内容被剥光）比例超过 1/3 → 停下报告，重新评估砍特性分支。
3. **198-keyless-full-chapter-production**（187 发现①拍板：写修复计划）：server/helpers/fallback-draft.ts 句式池多样性改造，消除 4000 字扩写下的 duplicate-sentence/mechanical-cadence/slop 78.4<85。Verify：node:test 确定性断言默认意图下 validateCompleteChapterDraftQuality ok=true、同 intent 输出仍确定、E2E real-pipeline spec 的 800 字 workaround 改回默认意图后仍绿。STOP：多样性改造后 slop 仍 <85 → 报告降档/豁免选项。
4. **199-stale-e2e-rewrites**：8 个陈旧 spec 逐个对照现行契约重写（每 spec 一个 Step + 独立 verify）：core-flow:131（确认并生成→生成本章正文+确认弹窗链，参考 agent-workspace-journey 现行链路）、unified-creation×3、plan150-confirmation、full-browser-click（总览 strict → switcher 锚定）、mobile×2。Verify：单 spec 绿 + 全套件不新增红。STOP：某 spec 的旧覆盖面已被新 journey spec 完全取代 → 报告后可改归档。
5. **200-skills-governance-columns**（192 实施前置）：server/lib/db/skills.ts:58 insertColumns 补 deck_group_id/deconstruction_card_type/sanitization_status/runtime_status/source_type/access_tier/is_runtime_ready 7 列（additive schema setup 模式）+ mapper + validateSkillPersistence 对齐。Verify：create/list roundtrip 字段持久化 + scripts/deck-pack-prototype.ts 断言转绿。注：涉及 schema additive 变更，计划内显式标注。
6. **201-cmdk-index-backfill**（194 §6 #1/#4）：编辑器正文自动保存 → 防抖批量回填 addChunk（序列化写、按 novel+chapter upsert）；模型代际变化时 overlay 呈现「索引已过期」诚实态（琥珀语义）。Verify：node:test upsert/代际守卫 + 手工冒烟步骤入文档。STOP：低端机实测单章 embedding >2s → 降频策略回报。
7. **202-config-sync-removal**：删 server/routes/config.ts:77 /api/config/sync 路由 + 改 tests/config-route-contract.test.ts:95；保留 updateCachedApiKey（user-flows 直调使用）。Verify：grep 0 引用 + 后端全量绿。
8. **203-slice-a-configuration-session**（195 切片 A）：configurationDraft/configurationDirty/staleConfigurationSession 三 state + 三段同步 effect（SkillsStudioView.tsx 现 1530-1558/1560-1590/1592-1692，自应用豁免窗口 1597-1607 `flagAge<5000` 语义必须原样保留）→ 新 src/stores/skills-configuration-store.ts。Verify：plan158 定向 38 用例 + 前端全量绿；props 62→~50 口径记录。
9. **204-slice-b-package-cluster**（195 切片 B）：packageSelections/pendingPackageSteps/packageSelectionDrafts/packageComponentResults/packageResultLaunchFeedbackAssetId 五 state（993-1004）+ 门控逻辑（1503-1520 等）→ src/stores/skills-package-store.ts；与 A 的 session 持久化耦合点（1654/1678/1708/1727）在计划中显式标注。A/B 可并行。
10. **205-slice-c-shelf-view-split**（195 切片 C，依赖 A）：savedSkills 加载（1146-1155）+ 货架分组调用侧（3353-3360）→ src/lib/hooks/useSkillsShelfData.ts；JSX 拆 CandidateTray/StyleShelf/PackageConfigDialog 到既有 src/components/skills/ 目录。Verify 同 8。
11. **206-small-debt-closeout**：server/routes/db.ts:380 导出临时文件名 Math.random → randomUUID（对齐 db-import.ts:1054 先例）+ /api/db/events（SSE notify）负载埋点调查（为 183 推迟的 generation 抑制提供数据，仿 184 的 searchSimilar 阈值埋点先例）。

## 台账与提交

- plans/README.md：状态表追加 196-206 行（含依赖：199 无依赖、205 依赖 203、200 是 192 实施前置）、Round 31 说明段（四个用户拍板方向 + 本批来源）。
- 提交：`docs(plans): round 31 — plans 196-206（185/陈旧spec/195切片/无Key修复/Vite7/CmdK后续/小额收口）`。

## 验证

- 11 个计划文件齐全、模板段落完整（Status/Why/Current state/Commands/Scope/Steps/Verify/STOP/Maintenance）；
- 台账行与文件一一对应、依赖图一致；
- 计划内锚点与勘察结果一致（抽查行号/文件存在性）。
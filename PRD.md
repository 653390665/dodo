# AI小说自动化工厂 (AI Automated Novel Factory) - 架构与开发计划

## 1. 核心护城河与理念 (Core Philosophy)
本平台致力于从“辅助工具”向“**全自动小说生产线**”演进。
在这里，AI 不是聊天的对象，而是各司其职的 **数字员工 (Agents)**。

### 1.1 人、AI、Agent 的边界与职责
为了让用户以**最小成本（Lowest Cognitive Load）**协作，我们需要明确划分权责：
- **User (人类制作人/总监)**：只负责“决策”与“验收”。提供高维创意（核心概念、世界观导向、主角人设），在各个关键节点（大纲、细纲、正文）执行“同意 / 拒绝 / 标注微调”的操作，**绝不亲自下场大段打字**。
- **Agents (全自动化流水线节点)**：负责繁重的“生成、自查、循环打磨”工作。多个专职 Agent 共生，形成闭环。
- **AI (底层大脑)**：提供大语言模型的基础理解与生成能力。

### 1.2 Agents 矩阵与自动化协同
- **Soul/Persona 机制**：每个 Agent 必须有深度的“灵魂设定”文档，限制其思维方式。
  - **Planner Agent (架构师)**：理性、严谨、懂网文黄金三章与悬念结构。负责将用户的一句话创意裂变成大纲，再拆解为本章细纲（Beats）。
  - **Writer Agent (主笔)**：感性、擅长画面渲染与情绪拉扯。读取细纲与 **Skill（技能卡）** 进行扩写。
  - **Critic Agent (毒舌主编)**：挑剔、无情。拿着设定集与网文红线（如不能送女、不能降智反派），对 Writer 的稿件进行扣分。如果低于 80 分，自动驳回并附带修改意见，让 Writer 重写（**内部循环，对用户透明**）。
  - **Evolution Agent (主技师)**：隐蔽在后台，收集用户的“修改痕迹”和 Critic 的打回记录，提炼成新的规则，更新给底层 Skill。

---

## 2. 核心系统壁垒 (Core Systems)

### 2.1 Skill 技能树：进化与变异引擎
**“Skill的选择极其正确且是核心壁垒”**。Prompt Engineering 的天花板明显，而动态加载的 Skill 机制能彻底改变这一现状。
- **本质**：Skill 是一组结构化的 JSON 配置，包含 [文风倾向]、[高频句式样例]、[绝对禁止红线(Negative Prompts)]。
- **进化 (Evolution)**：通过 Reflexion（反思）机制。当用户手动删除了 AI 写的一段“大段说教的设定”，Evolution Agent 会提取出“禁止在对话中加入大段旁白解说”，并写入该流派的 Skill。AI 越写越聪明。
- **变异 (Mutation)**：用户可以混搭 Skill，比如 [废土赛博朋克] + [日式轻小说吐槽] = 产生出一种变异的新反差文风。

### 2.2 去 AI 味法则 (De-AI Flavoring)
纯靠提示词很难洗掉 AI 味，必须在 Skill 层面加入**极其硬核的强制束缚**：
1. **词汇黑名单**：在 Skill 中内置“禁词表”（如：坚毅的眼神、不禁、宛如、宛若、倒吸一口凉气等 AI 高频词），一旦 Critic 发现，直接打回重修。
2. **句式打散**：强制要求长短句结合。动作戏必须是短句，景物烘托可以长句。
3. **Show, Don't Tell**：禁止使用总结性情绪词（如“他感到非常悲伤”），强制要求描写生理反应（如“他死死盯住地面，指甲嵌进了掌心，没有说话”）。

### 2.3 拆书工厂流程 (Book Deconstruction Flow)
*借鉴 GitHub 项目：可以参考 DSPy (用于提炼Few-Shot样例)、Text-Clustering 等。*
- **方法论与维度**：
  1. **结构维度 (Macro)**：通过长文本处理（如划分 Chunk），提取该作者每章的文字走向（日常->铺垫->冲突->断章/高潮）。
  2. **信息密度维度 (Density)**：分析在一段战斗中，动作占比多少？对话占比多少？内心独白占比多少？
  3. **微观句法维度 (Micro)**：提取具有原作者鲜明个人特色的 50 个经典句式组合（Few-Shot Examples）。
- **流程**：用户上传 TXT -> Chunk 分割 -> 投喂给分析模型 -> 输出 `<AuthorName>_Skill.json` -> 挂载到 Writer Agent。

---

## 3. 架构演进与开发计划 (Development Roadmap)

### Phase 1~2: 基础产品与追踪面板 (已完成✅)

### Phase 3: 全栈重构与 Agent 后端化 (Backend Orchestration) - 🟢 即将开始
- **目标**：脱离纯前端 React 生命周期，搭建真正的后端 Node.js (Express) 服务。
- **任务**：引入 LangGraph 构建**循环工作流**（Planner -> Writer -> Critic -> Writer...）。保证断网不掉稿，流程全托管。

### Phase 4: Soul 机制与 Persona 深度设定 (Agent Souls)
每个 Agent 都必须拥有极具深度的“灵魂设定 (Soul)”，这不只是简单的 System Prompt，而是包含了性格、网文经验认知、行为准则、避坑指南和思考方式的完整人格体。这些 Soul 设定文件将被硬编码或作为核心配置载入：

1. **Planner Agent (架构师/主编排) 的 Soul**：
   - **认知域**：“不要给我平庸的白日梦，给我能赚钱的钩子。”熟知起承转合、黄金三章、人物弧光和网文爽点节奏。
   - **行为准则**：在拆解大纲时，必须确保每个次要情节都服务于核心冲突；强制安排悬念断章；如果发现用户的设定逻辑不畅，能够礼貌但坚决地指出，并给出A/B策略供用户选择。
   - **思考模式(CoT)**：评估前情提要 -> 提炼核心爽点 -> 规划情绪曲线（先抑后扬） -> 输出带悬念的 Scene Beats。

2. **Writer Agent (主笔/大神写手) 的 Soul**：
   - **认知域**：“文字是用来唤起生理反应的，不是用来交待履历的。”精通视点聚焦（POV）、留白艺术、环境渲染烘托情绪。
   - **绝对红线 (Negative Soul Traits)**：彻底封印AI高频词汇（如：不禁、倒吸一口凉气、坚毅的眼神、宛如）；绝不越权脱离细纲擅自增加新主线；禁止总结性情绪泛用语（不说“他很悲伤”，而是描写“他一脚踢翻了椅子，眼周憋得通红”）。
   - **行为准则**：读取 Skill 模块的文风，根据当前场景的情感基调，灵活切换用词密度。战斗时短句快节奏，文戏时长句抒情。

3. **Critic Agent (毒舌总编/无情审稿机) 的 Soul**：
   - **认知域**：“读者的耐心只有三秒，前三行抓不住人就该撕掉。”熟读网文千万级别作家的毒点库，对逻辑狗屁不通、对话像念课文极其敏感。
   - **行为准则**：带着“找茬”的目的审视 Writer 的输出。核对设定库中的战力、物品树是否穿帮。
   - **输出模式**：采用极其毒舌且一针见血的风格。例如：“第二段的反派废话太多，他是个冷血杀手，不是个居委会大妈。打回，重写！建议直接砍掉对白，用动作开场。”

### Phase 5: 拆书工厂与 Skill 萃取台 (Book-to-Skill Studio)
- 开启新界面 `/skills`。用户上传 10 万字的小说，利用后端调度切割分析，产出结构化的能力卡牌。

### Phase 6: Hermes 技能进化模型 (Evolution System)
- 实现 Diff 追踪机制（用户改了哪些词），异步投喂给 Evolution Agent，让其每周一反思，输出《本周模型文风进化报告》。


# 历史架构草案：Cortex V2（非当前产品名称）

> 文档状态：历史架构草案。`Cortex` 是内部代号，不是当前产品名称；“主动式上下文管理”中的已实现部分已进入 InkFlow 的故事记忆、受治理候选和章节生产链。未实现条目仍是远期探索，不构成 Beta 交付承诺。当前定位与验证状态以 `README.md` 和 `docs/release-readiness.md` 为准。

## 一、 演进背景
在 V1 版本中，我们已经成功搭建了具备核心竞争力的“架构级 AI 辅助创作平台”，涵盖了设定大纲集、世界观圣经（World Bible）、分镜蓝图（Beats）、智能主笔与毒舌总编（Critic）的协同机制，以及首创的“叙事 DNA 插件（Skill 挂载）”。

为了应对长篇小说（动辄百万字）带来的上下文膨胀、逻辑崩坏以及管理困难，V2 架构需要从 **“被动式生成”** 升级为 **“主动式上下文管理与多维管控”**。

---

## 二、 核心架构升级点 (Architecture Upgrades)

### 1. 动态上下文矩阵 (Dynamic Context Matrix) - **【已规划】**
长篇小说无法在每次请求时携带所有的角色和场景设定。
- **痛点**：全量设定（Bible）注入会导致大模型上下文超载或注意力分散（Attention Dilution）。
- **优化架构**：引入 **“章节级局部变量 (Chapter Scope)”**。
- **功能点**：AI 会在生成细纲或审查草稿前，**自动抽取出本章活跃的实体**（角色、地点、道具）。用户也可以在侧边栏手动 Pin（固定）本章出场的人物。生成时，系统仅向大模型注入这些被激活的 Bible 数据。

### 2. 关系网与事件时间线 (Timeline & Event Tracker) - **【已规划】**
- **痛点**：缺乏时间维度的管理，会导致逻辑硬伤（例如：三天前刚断的手今天就能举剑）。
- **功能点**：在 World Bible 中新增 **“纪元/时间线”** 模块。每一个情节爆发点（Beat）都可以被打上时间戳或事件状态标签，形成可视化时间轴。

### 3. 可视化叙事蓝图 (Visual Narrative Board) - **【优化点】**
- **痛点**：目前的大纲（Global Outline）只是纯文本输入，不够结构化。
- **功能点**：建立网格化的**卷轴（Volume） - 章节（Chapter） - 分镜（Beats）** 三级树状管理，大纲侧边栏支持通过卷来折叠/展开章节。

### 4. 智能追踪台重构 (Trace & Sync) - **【即将实现】**
- **痛点**：废弃了原有的 Trace 空白页。
- **功能点**：将 Trace 改造为 **“内容审校与追踪台”**。在这里，AI 会根据小说上下文，自动检测最新一章是否提及了尚未记录在库的新角色/新地点，并**一键提醒用户生成 Bible 词条**（Auto-harvesting）。

---

## 三、 本次 Sprint 实施功能列表

为了快速落地 V2 的架构理念，本次将重点实现以下功能开发：

### Task 1: 侧边栏【追踪台 (Trace)】重生 —— AI 实体嗅探器
改造 EditorView 侧边栏的 `trace` Tab。
**功能描述**：
1. 提供一个“嗅探本章实体”按钮。
2. 点击后，AI 自动阅读本章分镜（Beats）和正文（Content）。
3. 提取出文中出现的专有名词（人名、地名、功法），并与现有的 World Bible 进行比对。
4. **分类展示**：
   - **已入库实体**：展示在本章中活跃的角色/据点。
   - **未入库/疑似新实体**：AI 发现文中出现了新名字，提示用户这是“野生设定”，提供一键“添加到设库”的快捷方式。

### Task 2: 章节上下文精准注入 (Context Pruning)
在请求 LLM 撰写和审查时，不再粗暴地传入全量 Bible。
**功能描述**：
结合 Task 1 的结果，在向主笔 (Writer) 和 总编 (Critic) 请求时，**仅传入与本章相关的设定**，在降低成本的同时极大提升 AI 的角色扮演精度。

### Task 3: 写作界面交互细节与大纲结构优化
增强编辑器体验，实现更加无缝的人机共创流。

---

## 四、 长期规划 (V3 Vision)
- 导出与排版中控 (Epub/TXT 编译)。
- RAG（检索增强生成）长期记忆存储，真正实现数百万字的无缝连贯。

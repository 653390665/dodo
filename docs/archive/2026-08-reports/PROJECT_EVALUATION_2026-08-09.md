# InkFlow 项目评估报告（2026-08-09）

> 评估基线：对比 2026-07-31 测试审查。本轮新增了类型检查、Lint、全量门禁重验，并对照 8/8 的《发布就绪报告》与《依赖升级评估》。

## 一、总体结论

**状态：工程成熟度高的「已验证 Beta 候选」，但被一个严重的"未提交"风险拖累。**

代码质量、测试门禁、安全审计全部达标，近期工作主线极其清晰（让所有失败可诊断、可恢复）。唯一且致命的问题是：**自 7/31 起零提交，当前 202 个文件 / 约 14,000 行的工程成果全部漂在未提交的工作树上**。Beta 已经"就绪"却从未真正进入版本库，一旦工作树损坏即前功尽弃。

---

## 二、当前健康指标（实测 + 8/8 报告）

| 维度 | 结果 | 来源 |
|------|------|------|
| 类型检查 `npm run typecheck` | **exit 0** | 今天（8/9）重跑实测 |
| Lint `npm run lint` | **exit 0** | 今天（8/9）重跑实测（加 `--max-old-space-size=4096`） |
| 后端单测 | **712 / 712 通过** | 8/8 发布就绪报告（当前整树重验进行中） |
| 前端单测 | **383 / 383 通过（63 文件）** | 8/8 发布就绪报告（当前整树重验进行中） |
| E2E（Playwright） | **10 / 10 通过** | 8/8 发布就绪报告 |
| 依赖安全 `npm audit --omit=dev` | **0 vulnerabilities** | 8/8 报告 |
| 后端覆盖率 | **91.40% 行 / 81.56% 分支 / 87.31% 函数**（≥90/80/85 门槛） | 8/8 报告 |
| 前端覆盖率 | **43.33%**（仅报告，不阻断） | 8/8 报告 |

> 备注：今天初次 `npm run typecheck` / `npm run lint` 触发了 SIGKILL（退出码 137），原因是沙箱默认内存上限不足以编译 9 万行项目；加 `--max-old-space-size=4096` 后双双 exit 0。**代码本身无问题**。

---

## 三、自 7/31 以来的方向演进

主线高度一致：**"让任何失败都可诊断、可恢复、可度量"**，工程化程度很高。

| Plan | 级别 | 内容 | 与审计/历史的关联 |
|------|------|------|------------------|
| **144** 测试稳定性收口 | P1 | 给 `npm test` 加 `--test-concurrency=4 --test-timeout=45000`、CI 加 15min 超时、新增 `export-route.test.ts`、统一临时目录 helper | **直接回应 7/31 审计的 P0/P1 发现** |
| **145** 空响应三分类 | P0 | 非流式/流式统一诊断：`no_content` / `reasoning_only` / `length_exhausted`，确定性失败只请求一次 | 解决"用户反复重试但不知为何失败" |
| **146** SSE 收敛 | P0 | 设定助手三套各自为政的 SSE 解析器收敛为共享 `readSseEvents()`；失败不再把错误当聊天消息堆叠 | 消除重复解析导致的不一致状态 |
| **147** 助手失败恢复 + 本地指标 | P1 | 两种助手失败后一键重试、不丢输入；本地漏斗指标（成功率 / 空响应率 / 5 分钟重试率 / 恢复 p50-p95，零样本显示 `null`） | 产品验收以"能否继续写"为第一指标 |
| 依赖加固 | — | vite / tailwind / @google/genai 可直升；`better-sqlite3` / react / radix **严禁动**（ABI 地雷） | 见《依赖升级评估》 |

---

## 四、我的测试审计发现是否被处理

| 7/31 审计发现 | 当前状态 |
|--------------|----------|
| **P0** 默认 `npm test` 并发挂死（18 分钟无输出） | ✅ **已修**：144 加了并发上限 + 单测超时 + CI 超时，今天实测可正常跑完 |
| **P1** `export.ts`（WAL 备份下载）零测试覆盖 | ✅ **已修**：144 新增 `tests/export-route.test.ts`，覆盖 TXT/EPUB 与真实 `db.backup()` 快照清理/降级 |
| **P0/P1** 部分测试硬编码固定 DB/配置路径 | ✅ **已修**：144 新增 `tests/helpers/test-environment.ts`，用 `mkdtempSync` 唯一目录并恢复环境变量 |
| **P1** 37 个顶层组件中仅 14 个有测试 | ⚠️ **部分接受**：144 计划明确反驳——审计的组件计数不准、且无具体回归佐证，故暂不机械补齐；前端覆盖率 43% 作为已知限制接受 |
| **P2** 薄弱断言（`toBeTruthy()` 居多） | 🔶 未专门处理，但 145–147 新增的测试显著强化了空响应/失败路径的精确断言 |

**结论：审计的核心工程风险（并发挂死、导出零覆盖、固定路径）已被实质解决，方向被团队采纳。**

---

## 五、风险清单（按优先级）

### 🔴 R0 — 未提交的工作树（严重，最高优先）
- **202 个文件、126 文件变更、9,684 增 / 4,474 删** 全部未提交；git 自 7/31 起 **零提交**（总计仅 12 个提交）。
- 包含：144–147 全部成果、一次依赖升级（package.json / lock / 评估文档）、发布就绪报告等。
- **影响**：Beta 已"就绪"却从未入版本库。机器损坏、误操作 `git reset --hard`、分支切换都可能让近一个月的工程付诸东流。
- **建议**：**立即保全**——至少 `git stash`（或创建 WIP 提交 / 分支），再考虑如何整理成可审阅的提交。发布前必须入库。

### 🟡 R1 — 前端覆盖率偏低且不设门槛
- 前端仅 43.33%，且明确"仅作报告、不阻断"。大块 UI（37 个顶层组件）无回归保护。
- 已声明为接受项，但意味着 UI 重构的回归风险完全靠人工 / E2E 兜底。

### 🟡 R2 — 发布就绪度缺口（外部依赖）
- macOS 包**未签名**、使用默认图标；**Windows 包本轮未重验**；真实 LLM Provider 可用性依赖用户配置/网络/上游。
- 这些不在代码层面，但决定 Beta 能否真正交付。

### 🟢 R3 — 依赖升级 ABI 地雷（技术债）
- `better-sqlite3`、`react`、`@radix-ui/*` 升级会触发 Electron Native ABI 不匹配 / Ref 兼容崩溃。已有严格升级流程文档，但仍是高危操作，需专人 + 打包机验证。

---

## 六、建议的下一步

1. **【立即】保全工作树**：`git stash` 或创建 WIP 分支提交，消除数据丢失风险。
2. **逐文件对齐 8/8 基线**：确认 8/8 之后新增的改动已重新入门禁（本次整树重验即为此目的）。
3. **发布决策**：签名 macOS、补验 Windows、在目标环境验证真实 Provider 后，再宣布 Beta。
4. **可选**：前端覆盖率至少在 CI 中以"观察性"门槛报告，避免无声下滑。

---

## 七、一句话总结

> 代码与测试都很扎实，方向也对；**唯一要立刻做的是把这 200 多个文件存进版本库**——否则一个 Beta 候选只活在本地未提交目录里，是最不该有的风险。

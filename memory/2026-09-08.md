# 2026-09-08

- 核实轮(用户逐项验证):三处失真确认并修正——① README 011 行滞后,已更新为 Phase1-4+5a、props 97→66、Phase 5b 另立评估;② 011 账目 mountedSkillLoadout 由「@deprecated」改为「遗留待清」(真实 @deprecated 在 book-factory QualityTab.tsx:32;AW 侧仍被 getProjectCapabilityCardIds 活跃消费,plan158 测试断言);③ 57/57 归属 research 联动块测试而非 010,010 文档无需改。
- 上述两文件修正已暂存未提交;pre-commit 因本机 npm 缺失失败(node 安装损坏),等修复或 --no-verify 授权。
- OpenSquilla 配置(config.toml,已备份 .bak-20260908):`llm.context_window_tokens=500000`(原 0=自动;GLM-5.3-flash 实际窗口 1M,OpenRouter 标 1310720,500k+128k 输出余量充足)、`memory.flush_pre_compaction=true`、`memory.session_source_enabled=true`、`compaction.protected_recent_messages=2`。TOML 已验证合法;需重启网关生效。
- 搜索环境:duckduckgo 稳定超时;bocha/brave/exa/tavily/iqs 均缺 API key。z.ai 文档可 `web_fetch` 直抓 `.md` 端点(如 docs.z.ai/guides/vlm/glm-5.3-flash.md)绕过。

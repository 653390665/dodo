# Plan 122：建立可等待的编辑器保存边界

## 状态

`DONE`

## 范围

- 按实体与字段键控正文、分镜、大纲、标题、卷名写入。
- 提供可重试的 `flushPendingEditorWrites()`；失败保留 pending 且不显示同步成功。
- 章节、项目与视图切换前显式等待保存边界。
- Electron 首次关闭握手、5 秒超时放行与浏览器 `beforeunload` 提醒。

## 完成条件

全部门禁及隔离 SQLite 的“立即退出后重启仍保留最后输入”测试通过后标记 `DONE`。

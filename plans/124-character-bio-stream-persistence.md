# Plan 124：人物小传流式预览只落盘一次

## 状态

`IN PROGRESS`

## 范围

- 复用严格 SSE reader，要求合法 JSON 与 `[DONE]`。
- token 仅节流更新本地预览；完成后单次更新数据库。
- 断流、解析错误、服务端错误与 Abort 恢复原值；旧请求不得覆盖新结果。

## 完成条件

成功、失败、Abort 与乱序测试及全部门禁通过后标记 `DONE`。

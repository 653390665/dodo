# Plan 125：导入前验证 SQLite 完整性与 schema

## 状态

`DONE`

## 范围

- 上传内容先写入唯一、私有权限的验证临时文件。
- 独立只读连接执行 quick check、外键数据与声明、核心 schema 和关键查询验证。
- 预检通过后才进入串行替换；所有失败保留主库并清理临时文件。
- 客户端仅返回通用错误，详细原因只记录到服务端日志。

## 完成条件

损坏、伪造、缺列、外键异常、旧备份迁移和残留清理测试及全部门禁通过后标记 `DONE`。

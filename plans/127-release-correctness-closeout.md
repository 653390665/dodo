# Plan 127：发布正确性收口

## 状态

`DONE`

## 范围

- 正文生成使用严格 SSE 状态机；业务错误、坏 JSON 与缺少 `done` 均恢复原文且不得落盘、建版本或记录成功。
- 灵感碎片展开要求 `[DONE]`，按碎片隔离请求并阻止并发和迟到结果覆盖。
- 保存版本读取 flush 后的最新编辑器快照；应用快捷键统一经过保存门禁。
- 正文保存失败时禁止继续生成保底分镜；生成正文落盘后辅助记录失败不得回滚正文。
- 零摩擦设定补录真实写入人物、地点或道具表，失败不得显示同步成功。
- 数据库代理为核心写作实体增加字段级运行时校验；导入备份最多保留 5 份。
- 修复设定工坊导航与 Electron 关于窗口版本漂移。

## 完成条件

`npm run typecheck`、`npm run lint`、`npm test`、`npm run test:frontend`、`npx playwright test`、`git diff --check` 全部通过；CI 的 macOS/Windows 打包生命周期测试均已通过。

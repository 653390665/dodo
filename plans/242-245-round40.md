# Round 40：断链修复 + 交互收敛（统筹批）

> 四个计划按依赖序执行：242（解锁配卡）→ 243（交付路径）→ 244（门禁与沟通）→ 245（交互收敛）。242/243 为 P0。

## 242: 货架 manifest 全量登记（P0，解锁 106 卡配置链路）

- **根因**：`cloneAssetToSkill` 等治理链路走 `getCatalogCapabilityManifest`（CURATED_DEFINITIONS 表），而文风货架 106 张卡（square-*/private-*/creative-*/sanitized-* 等）不在表中 → undefined → 配置静默失败。
- **修法**：`capability-manifest-catalog.ts` 增派生层——对 PROMPT_GOVERNANCE_CATALOG 中 `runtimeStatus==='active' && placementTier==='optional-style'` 的条目，按 stage 派生 technique manifest（planning/foundation→planner、review→critic、其余→writer；scopes project+chapter；usageModes persistent-rule+single-run）；`sanitized-*` 副本剥前缀取源条目、runtimeStatus 恒 active。惰性索引，不改 CURATED_DEFINITIONS。
- **Verify**：货架卡 manifest 覆盖率 106/106；浏览器实测散卡「设为作品默认」发出 API 且 DB projectTechniqueIds 非空；回归绿。

## 243: 接受写入丢稿修复（P0）

- **取证**：接受后 chapters.content 长度 = 0（DB 实锤），编辑器空、立即审查审到分镜提纲。
- **修法**：定位生产接受的写入链（preview accept → chapter.content），修复来源校验/写入目标； Verify：浏览器接受后 DB content 非空且与预览一致、编辑器字数同步。

## 244: 门禁沟通与误杀校准（P1）

- 超时降级时给用户明确 toast/状态（ProviderError timeout → "生成服务响应超时，已降级，请重试"）。
- 质量门"缺 Beat/Narration"启发式接入证据核对（有动作穿插不判缺），减少误杀。

## 245: 交互收敛快赢（P2）

- 生成入口归一（四按钮 → 步骤条主按钮 + 快速模式开关）；
- 未选作品静默返回 → toast + 跳书库引导；
- 侧边栏折叠组展开状态持久化；
- 术语人话化（索引降级→搜索索引待重建 等）。

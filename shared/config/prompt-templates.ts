import type { PromptTemplateKey } from '../types';

export type { PromptTemplateKey } from '../types';

export type PromptTemplates = Record<PromptTemplateKey, string>;

export interface PromptTemplateDefinition {
  key: PromptTemplateKey;
  label: string;
  description: string;
  variables: string[];
}

export const PROMPT_TEMPLATE_DEFINITIONS: PromptTemplateDefinition[] = [
  {
    key: 'inspirationSystem',
    label: '灵感助手',
    description: '控制灵感助手的系统身份与回答风格。',
    variables: [],
  },
  {
    key: 'storyCards',
    label: '故事方案卡',
    description: '根据灵感对话结果生成 3 张可选故事方案卡。',
    variables: ['ideaSeed', 'chatContext', 'expectedWordCount', 'storyFocus', 'pacingPreference'],
  },
  {
    key: 'setupTaskRefine',
    label: '设定项细化',
    description: '围绕单个设定任务继续细化草稿。',
    variables: ['taskTitle', 'currentDraft', 'userRequest', 'storyContext'],
  },
  {
    key: 'editorAgent',
    label: '分镜生成',
    description: '把用户意图拆解成场景分镜。',
    variables: ['PLANNER_SOUL', 'contextStr', 'skillsInfo', 'userIntent'],
  },
  {
    key: 'manualAudit',
    label: 'AI 审计',
    description: '创作舞台里手动触发的审计提示词。',
    variables: ['contextStr', 'skillsInfo', 'sceneBeats', 'draftContent'],
  },
  {
    key: 'orchestrateWriter',
    label: '正文生成',
    description: '正文生成主链路的 Writer 提示词。',
    variables: ['WRITER_SOUL', 'contextStr', 'skillsInfo', 'sceneBeats', 'criticFeedback'],
  },
  {
    key: 'orchestrateCritic',
    label: '正文生成内审',
    description: '正文生成链路中用于回看初稿的 Critic 提示词。',
    variables: ['CRITIC_SOUL', 'contextStr', 'skillsInfo', 'sceneBeats', 'currentDraft'],
  },
  {
    key: 'extractSkill',
    label: '拆书萃取',
    description: '拆书工厂里从样本文本萃取拆书卡的模板。',
    variables: ['text'],
  },
  {
    key: 'generateOutline',
    label: '全局大纲',
    description: '生成卷轴级全局大纲的模板。',
    variables: ['expectedWordCount', 'title', 'worldRules', 'seedOutline'],
  },
];

export const DEFAULT_PROMPT_TEMPLATES: PromptTemplates = {
  inspirationSystem: `
# 角色
你是一个资深网文策划编辑兼创作合伙人，带过十几本从零到万订的作品。你有敏锐的故事判断力——能从一句模糊的念头里嗅到可能性，也能温柔地指出一个方向为什么不成立。

# 行为准则
1. 不要直接输出完整故事——你是在帮作者找到他/她想写的故事，不是在替他写。
2. 每次回应给 2-3 个不同方向的选择，而不是一个"最佳方案"。创作没有唯一正确答案。
3. 指出方向时，必须同时说明这个方向的风险（最容易写崩的点）和潜力（为什么读者会追读）。
4. 当用户的念头过于模糊时，用提问帮他聚焦——"你更想写一个人的成长，还是一群人的博弈？"
5. 语言风格：像深夜聊创作时坐在对面沙发上的伙伴——专业但不冷，热情但不吹捧。
6. 记住对话历史中的关键偏好（用户选择了什么方向、否定了什么），在后续建议中体现出来。
`.trim(),
  storyCards: `
你是网文策划编辑。根据用户输入判断是否能生成故事方案卡。

【用户输入】
{{ideaSeed}}

【写作约束】
目标字数：{{expectedWordCount}}字 | 侧重：{{storyFocus}} | 节奏：{{pacingPreference}}

【判断规则 — 必须严格执行】
先判定输入是否包含可识别的故事元素（具体角色、场景、冲突、情绪、世界观信号）。满足以下任一条件时，必须输出 status:"needs_clarification" 而非 story cards：

1. 输入是纯数字、纯拼音、英文乱码、无意义单字重复（如"111111""啊啊啊啊""asdfgh"）
2. 输入中没有任何可识别的故事信号——只有虚词、问候语、或语义空洞的文字
3. 输入只有一个孤立的词（即使这个词有意义），缺少场景、冲突或人物关系
4. 输入看起来像测试文本、随口应付（如"随便写一个""不知道""测试一下"）

仅当输入确实包含故事元素时，输出 status:"ok" + 3张卡。

两种输出格式：

格式1 — 输入可用（精简输出，只输出创意核心字段，结构字段由系统自动补齐）：
{
  "status": "ok",
  "cards": [
    {
      "hook": "≤30字。必须包含用户输入中的关键概念/元素。禁止使用'一个关于X的传奇故事''命运的X篇章''一段X之旅'等空洞模板。每个hook必须是具体的、独特的故事钩子。",
      "protagonist": "主角人设（身份+性格+动机）",
      "coreConflict": "核心冲突（谁vs谁，因为什么）",
      "tone": "故事气质（冷峻悬疑/热血逆袭/慢热铺陈/黑色幽默/史诗悲歌/轻快日常）",
      "whyItWorks": "为什么值得写（必须引用输入中的具体元素）",
      "riskNote": "最容易写崩的点",
      "mixTags": ["2-4字标签"]
    }
  ]
}
注意：以上 3 张卡，3 张必须有明显不同的方向。

格式2 — 输入不可用：
{
  "status": "needs_clarification",
  "questions": ["引导问题1", "引导问题2"]
}

硬性规则：
1. hook不能是空洞模板——每个hook必须包含输入中的具体概念，让读者一看就知道这个故事跟输入有关
2. 3张卡的hook必须有明显不同的切入角度，不能是同一个方向换了几个词
3. 禁止输出正文片段、禁止markdown、禁止think标签
4. 只输出合法JSON，不要前后缀
5. 不确定能不能用的时候，倾向于输出 needs_clarification
  `.trim(),
  setupTaskRefine: `
小说设定协作助手。针对单个设定项给出更稳的改写。

输入：{{taskTitle}} | 草稿：{{currentDraft}} | 上下文：{{storyContext}} | 方向：{{userRequest}}

输出严格JSON：
{
  "result": "≤150字改写结果",
  "changedFields": ["补足的维度名1", "补足的维度名2"],
  "reason": "≤50字说明为什么这样改"
}

规则：不输出问答/大纲/符号列表，优先补动机+限制+后果。
  `.trim(),
  editorAgent: `
{{PLANNER_SOUL}}

【当前启用的能力卡规则】
{{skillsInfo}}

【当前任务】
利用以下小说的信息记忆库，根据用户的创作意图，拆解出这一章的场景分镜。

{{contextStr}}

【用户本章创作意图】
{{userIntent}}

【输出要求】
请输出 3-5 个场景分镜，每个场景严格按以下结构：

### 场景 N：场景名称（不超过 8 字）

**出场人物**（≤20字）：列出本场景涉及的已知角色名
**入场钩子**（≤30字）：一句话说明场景从什么时刻/动作/画面开始
**核心冲突**（≤40字）：谁和谁因为什么产生张力
**关键动作链**（≤60字）：2-3个必须在本场景发生的动作/事件
**关键道具/信息**（≤30字）：本场景必须出现或传递的物件、线索
**情绪转折**（≤20字）：开始情绪→结束情绪
**退场钩子**（≤30字）：场景由什么动作/画面/声音结束
**连接上一场景**（≤20字）：如何承接上一个场景的结果

---

硬约束：
1. 每个场景必须有清晰的入场和退场
2. 关键动作链必须是可以被写出来的具体动作
3. 退场钩子不能重复入场钩子
4. 每个场景的冲突类型不能全部相同——至少包含两种不同类型
5. 道具必须和动作绑定
6. 每个场景必须覆盖分镜要求的至少2个关键动作，未覆盖视为FAIL
  `.trim(),
  manualAudit: `
金牌总编·结构化审计。只评估输入正文，不要改写正文。

【世界观】{{contextStr}}
【能力卡规则】{{skillsInfo}}
【分镜】{{sceneBeats}}
【正文窗口（包含 opening/middle/ending 原文位置标签）】{{draftContent}}

检查目标→阻碍→选择→代价→后果、铺垫→过程→余波、场景收束、段落同构和可读性。正文中出现“作品：”“作者：”“章节标题”“提纲：”“问：/答：”等创作元数据或问答残留时，必须作为 fatalIssues 报告，不能当作小说正文接受。fatalIssues.snippet 必须逐字来自窗口中的原文；窗口是非连续片段，不得把分隔线当正文。

只输出一个 JSON 对象。合法示例（仅示范字段形状，数值必须按正文重算）：
{"scores":{"可读性":{"score":8,"reason":"句式完整，指代清楚"},"分镜执行度":{"score":7,"reason":"第二场景的道具落点偏弱"},"冲突推进度":{"score":8,"reason":"门外脚步改变了选择"},"风格契合度":{"score":7,"reason":"短句节奏稳定"},"网文章节感":{"score":8,"reason":"章末钩子改变局势"}},"totalScore":38,"pass":true,"failReason":"","fatalIssues":[],"surgerySuggestions":["补足第二场景道具动作"]}

scores 只能包含可读性、分镜执行度、冲突推进度、风格契合度、网文章节感五项，每项 score 为 0-10 数字，reason ≤50字；totalScore 必须等于五项之和。pass 只能为 true 或 false；failReason ≤80字。硬阻断：任一维度<4或 totalScore<30 时 pass=false、failReason 必填且 fatalIssues至少1条，最多三条。正文含创作元数据或问答残留时，即使五维分数较高也必须 pass=false 并至少报告一条对应 fatalIssues。每条 fatalIssues 必须完整包含 issueType、issueSubtype、severity、snippet、explanation、patchHint；explanation 和 patchHint 各 ≤50字。无明确问题时 fatalIssues 必须为 []。禁止 markdown、前后说明和思考标签。
  `.trim(),
  orchestrateWriter: `
{{WRITER_SOUL}}

【世界观与人物志】
{{contextStr}}

【能力卡规则】
{{skillsInfo}}

【分镜蓝图 (Beats)】
{{sceneBeats}}

【总编的审读建议】
{{criticFeedback}}

---

你现在不是在写提纲、便签或残缺草稿，而是在写一段“可以直接被读者阅读的网文章节正文”。

请严格遵守以下硬规则：
1. **按分镜顺序展开**：从第一个场景写到最后一个场景，不要跳场，不要把多个场景揉成一团。
2. **必须写成完整中文句子**：禁止残句、病句、半截意象、语义断裂、主谓宾缺失。
3. **每一段都要交代清楚**：是谁在做什么、看到了什么、听到了什么、局势发生了什么变化。
4. **允许有风格，但先保证可读性**：先写通顺，再使用能力卡中的风格意象、句式倾向和高频词。
5. **对话必须带动作或观察支撑**：不要只让角色干说信息，要让对白与神态、环境、停顿、试探同时发生。
6. **保持单一视角和连续叙事**：不要突然改成人物小传、作者解释、纲要总结。
7. **把冲突往前推**：每个场景都必须比上一个场景更接近真相或更接近危险。
8. **结尾必须收在分镜要求的悬念点**：不要提前把钩子说破。
9. **人物称谓要自然切换**：人物在开头点明一次身份、绰号或外观称谓后，后文优先使用“他”“那人”“掌柜”等自然指代。
10. **正文里禁止把人物写成”主角”**：除非用户明确要求写元叙事，否则不准出现”主角”二字。任何情况下不准用”主角”指代人物——没有正式名字时，必须改写成”那人””那青年””那江湖客””那书生””那汉子”等正文内称谓，根据角色的年龄、身份、气质选一个，后续用代词”他”和身份词交替承接，不准重复同一个称谓。写出”主角”即视为不合格，必须整句重写。
11. **起笔必须是动态入场**：第一句优先写声音、动作、碰撞或异动，不要用中性天气播报式句子平平开场。
12. **危险动作要藏，不要解说**：不要直接替读者解释“他在警戒”“他要拔刀”，要通过手势、停顿、目光、呼吸、物件位置让读者自己看懂。
13. **关键信息要分两步释放**：任何会改变局势判断的信息，先给异样、试探或铺垫，再给明确揭露；不要让“三天”“令牌”“钥匙”这类关键信息半截砸下来。
14. **任何对白都必须有前因**：在人物开口说话前（包括第一句和每一句关键台词），必须先交代促使他开口的观察、停顿、呼吸变化、肢体试探、压力感知或判断失误。不能让人物"突然说了一句"——读者必须先感受到"他为什么在这个时刻开口"。对白不是信息广播，是人物在特定压力下的选择。
15. **关键道具和动作必须兑现**：分镜里点名的道具、视线变化、手势试探、站位变化，不准只写气氛不写落点，必须在正文里真正发生并推动局势。
16. **少解释，多显影**：如果一句话是在替读者解释人物意图、危险程度或关系变化，优先改写成动作、对话、物件位置或他人反应，让读者自己读出来。
17. **场景收束禁用套路收尾**：禁止用"转身离去""转身离开""渐行渐远""消失在夜色中""消失在人群里""目送他远去""身影渐渐模糊"等套路化收束。每个场景的收尾必须是本场景独有的——用本场景特有的动作、声音、物件或环境异变来终结，让读者感受到"这个场景到此为止"，而不是套一个万用模板。
18. **输出必须是无格式纯正文**：只输出连续的自然段，禁止任何 Markdown 或排版标记——不允许出现章节标题、"#"、加粗星号 "**"、斜体、分隔线 "---"、编号列表、"1."、"场景X："等前缀、引用符号 ">"、方括号标注。所有标记类字符都视为不合格输出，必须整段重写为纯叙事文本。
19. **禁用翻案腔解释句**：不要写“不是A，而是B”“看似A，实则B”“与其说A，不如说B”这类对比翻案句式。人物动机、局势判断用动作、后果和他人的反应呈现，不用对比句式替读者下结论。
20. **禁用破折号揭晓式停顿**：不要用破折号制造悬念揭示（如“他推开门——桌上摆着那封信”）。信息揭示让动作先落地，用句号分段呈现；破折号全章至多出现一次，且不得用于揭晓或解释。

正向场景合同（每个场景逐项兑现）：
- 至少完成一次“目标→阻碍→选择→代价→可见后果”，且后果改变下一段的行动或风险。
- 关键事件形成“铺垫→过程→余波”；环境描写必须改变行动、信息或风险，不能只做气氛填充。
- 对白由具体压力触发，先有观察、停顿或试探，再让人物开口；对白不能替作者解释设定。
- 章节收尾必须改变局势判断，留下本场独有的动作、声音、物件或环境异变。

质量底线：
- 正文至少要像“章节初稿”，不能像“AI 生成坏掉的碎片”。
- 默认目标为 4000 字以上；如果分镜信息较少，也至少写出一段结构完整、可读性稳定的正文。
- 不要输出标题、说明、注释、分点、Markdown 表格或任何额外解释。
- 如果正文里某个代词、称谓、解释句连续出现三次以上，请主动改写，避免机械重复。
- 如果你写出了”主角”二字，说明这一版不合格，必须立刻改写整句。
- 如果场景结尾使用了”转身离去””渐行渐远””消失在XX中”等套路收束，说明这一版不合格，必须用本场景特有的动作/声音/物件改写收尾。
- 如果关键信息是通过对白给出的，必须先让上下文把这句对白“托住”；不要出现信息正确但读起来突兀、像硬塞设定的句子。
- 如果分镜要求出现关键道具或关键试探动作，而正文里没有真实兑现，说明这一版不合格，必须回补到可读的场景动作链里。

写作方法：
1. 开头第一句先给“第一帧”：声音、动作、门响、视线碰撞、异物坠地，至少占一种。
2. 再用 1-2 段完成场景落位，让读者知道地点、气味、声音、光线和人物站位。
3. 人物命名只在必要处点一次；如果未提供名字，请先给出一个不突兀的正文内称谓，再用代词、身份、动作承接，绝不要整章反复写“主角”。
4. 让人物通过动作、观察、对话推进试探，而不是直接交代结论；任何对白前先给一个促发动作或认知触发点——停顿、目光游移、呼吸收紧、物件触碰，至少占一种。
5. 关键信息必须通过冲突浮出水面，不要平铺直叙地宣布答案；先让人感到不对，再揭示不对在哪里。
6. 分镜里点名的关键道具、关键手势、关键站位，至少要有一次被正文明确写出并产生作用。
7. 写到危险、猜疑、出手前一瞬时，要留半步空白，不要替读者把潜台词说完。
8. 结尾用本场景特有的动作、声音或环境异变收束，让悬念自然落下。禁用"转身离去""渐行渐远""消失在夜色中"等通用模板——每个场景的收束必须独一无二，只能属于这个场景。

请直接输出正文，不要附加任何解释。

输出正文前，逐条自检（必须全部满足，否则重写）：
□ 首句含声音/动作/碰撞/异动（非天气播报）
□ 正文未出现"主角"二字
□ 分镜要求的道具已在正文出现至少1次
□ 每一处对白前都有观察/停顿/试探/促发动作作前因
□ 场景收束为本场景特有动作/声音/物件，未使用"转身离去"等套路模板
□ 无残句、病句、主谓不明
□ 未出现"不是…而是…""看似…实则…"等翻案腔句式
□ 未用破折号制造揭晓式停顿
  `.trim(),
  orchestrateCritic: `
{{CRITIC_SOUL}}

【背景架构】
{{contextStr}}

【预期能力卡规则】
{{skillsInfo}}

【本节分镜要点 (Beats)】
{{sceneBeats}}

【待审计的正文草稿 (Current Draft)】
{{currentDraft}}

---

你现在的身份是“金牌内容总编”，目标是快速判断这份正文能不能进入下一轮。

请按下面标准执行内审：
1. **可读性底线**：是否存在残句、病句、主谓不明、逻辑断裂、指代混乱。
2. **分镜执行度**：是否真的按 Beats 写了出来，还是只写成了松散片段。
3. **冲突推进度**：每个场景有没有推进信息、风险或关系变化。
4. **风格契合度**：是否真正吸收了能力卡规定的笔调，而不是只贴几个词。
5. **网文章节感**：读起来是否像可发布章节初稿，而不是提纲扩写失败的毛坯。
6. **语言老练度**：是否存在“主角、主角、主角”这类机械称谓重复，甚至直接把人物写成“主角”；开头是否像天气播报；危险动作是否被作者直接解释。
7. **场景合同**：逐场核对目标、阻碍、选择、代价、后果，以及关键事件的铺垫、过程、余波；缺失时必须引用具体段落。
8. **结构同构**：检查段首、段末、主语动作链和“气氛→解释→悬念”骨架是否高密度重复；少量相似句只作 P2 建议，连续高密度才可列为 P1。

判定规则：
- 若评分 >= 80，且不存在“残句/严重逻辑断裂/明显未执行分镜”，首行输出 PASS。
- 否则首行输出 FAIL。

输出格式必须严格如下：
PASS 或 FAIL

## 评分
- XX/100

## 致命问题
- 列出最影响可读性的 2-5 个问题，必须引用原文片段并说明为什么糟糕。

## 分镜执行检查
- 逐条说明哪些场景写到了，哪些场景被跳过或写弱了。

## 手术建议
- 给出可以直接指导下一轮重写的具体建议，不要空泛鼓励。

禁止空泛表扬。必须给出能直接指导重写的判断。
  `.trim(),
  extractSkill: `
你是一个顶级的网文”架构级”拆书专家。你的任务是从一段范例文本中萃取可组合的拆书卡组，每张卡目标单一、边界清晰。

===== 第一部分：输入有效性自检（必须最先执行） =====

在开始拆书之前，你必须先判断输入文本是否具备可拆解性。满足以下任一条件时，必须拒绝拆书，输出 needs_clarification 而非能力卡 JSON：

1. 文本中几乎没有中文叙事内容（纯标点、纯数字、纯英文、乱码、测试文本）
2. 中文内容过短（有效中文字数 < 50 字），无法提取稳定的风格特征
3. 文本完全是对话记录、评论、说明文字，不是叙事性小说文本
4. 文本由同一句话机械重复构成，没有实际的叙事段落
5. 文本的语言特征极其平淡（如：纯粹的新闻稿、说明书、技术文档），没有可抽取的文学风格信号

自检通过条件：文本必须包含 ≥ 50 个有效中文字符，且其中包含至少一个可识别的叙事信号（具体场景描写、人物动作、对话、环境渲染、情节推进中的任一种）。

===== 第二部分：拆书执行（仅当自检通过后执行） =====

拆解维度（你必须将萃取的卡牌映射到以下 7 类专业“拆书卡”中，并在输出的 deconstructionCardType 字段中体现）：
1. worldview-card：世界观与设定卡。包含力量体系、核心设定、世界规则背景等。
2. character-card：人物张力卡。包含核心角色说话基调、互动和行为逻辑。
3. pacing-card：节奏调速卡。控制字数节奏、分镜信息释放密度。
4. hook-card：开篇钩子卡。黄金三章核心悬念、入场吸引力。
5. conflict-card：冲突递进卡。场景张力升级、矛盾爆发。
6. style-card：风格笔调卡。意象、短句、动作细节描写规约。
7. platform-card：平台偏好卡。番茄完读率、起点爽点、Webnovel 海外偏好等。

在确定 primaryDimension 时：
- style-card 对应 style
- character-card 对应 character
- worldview-card 对应 world
- pacing-card 对应 pacing
- conflict-card 或 hook-card 对应 plot
- platform-card 对应 style 或 plot

拆解原则：
1. 输出 2-4 张卡。如果文本在某个维度信息不足，不要强行生成。
2. 强约束：实体白标化。必须在 description、style、characterTraits、fewShots 等所有输出字段中彻底抽离出“通用技法”，彻底清除并过滤任何具体小说的实体泄露。必须移除所有知名小说主角名（如：萧炎、唐三、石昊、叶凡、韩立、方源、林动等）以及占位泄漏名（如：林天凡、楚天凡），用通用指代（如“那人”、“那白衣青年”）代替。
3. 强约束：杜绝 AI 套话。绝对禁止在输出字段中使用以下万能套话词汇：“文笔流畅”、“描写细腻”、“人物形象鲜明”、“情节紧凑”、“引人入胜”、“语言精炼”、“意象丰富”、“跃然纸上”及其近义词。若有类似特征，请用具体、可验证、不落俗套的句式结构、修辞或机制描述代替。

===== 第三部分：输出格式 =====

【自检不通过时】严格输出：
{
  “status”: “needs_clarification”,
  “reason”: “≤80字，具体说明为什么这段文本无法拆书”
}

【自检通过时】严格按以下 JSON 格式输出：
{
  “status”: “ok”,
  “skills”: [
    {
      “name”: “卡名（简洁有力，如：冷雨短句刀锋文风）”,
      “primaryDimension”: “style | character | world | plot | pacing 之一”,
      “deconstructionCardType”: “worldview-card | character-card | pacing-card | hook-card | conflict-card | style-card | platform-card 之一”,
      “description”: “一句话说明这张卡负责什么（移除任何小说实体名）”,
      “style”: “文笔风格描述（如与本卡无关则填空字符串）”,
      “pacing”: “节奏逻辑（如与本卡无关则填空字符串）”,
      “characterTraits”: “人物构建特征（如与本卡无关则填空字符串）”,
      “worldBuilding”: “世界观/力量体系特征（如与本卡无关则填空字符串）”,
      “plotPattern”: “剧情套路特征（如与本卡无关则填空字符串）”,
      “foreshadowing”: “伏笔与悬念手法（如与本卡无关则填空字符串）”,
      “corePatterns”: [“3-5个核心模式标签”],
      “bannedElements”: [“会破坏这张卡基调的词汇或设定”],
      “vocabulary”: [“高辨识度的通用词汇，移除实体词”],
      “fewShots”: [“移除实体名白标化后的、最能体现这一维能力的代表性片段，且字符数必须大于20个字”],
      “blendHints”: [“更适合与哪类卡搭配的组合建议”]
    }
  ]
}

硬性规则：
1. 不要包含 Markdown 代码块标记（\`\`\`json 等）
2. 只输出上述 JSON，不要有任何前后缀
3. 不确定是否能拆时，倾向于输出 needs_clarification
4. 禁止在任何字段出现上面列出的知名实体和 AI 腔套话字眼。

以下为分析素材：
{{text}}
  `.trim(),
  generateOutline: `
你是一个顶级的网文主编及架构师。用户正在进行长篇小说的架构规划。
小说的预计总字数是：{{expectedWordCount}}字。
{{title}}
{{worldRules}}
{{seedOutline}}

请根据预计总字数，将整部小说合理地划分为几个大卷（或大情节弧线），明确规划出每卷的内容梗概、字数分配以及章回数量预测。
规划必须结构清晰、节奏合理，符合网文商业大纲的标准（如：起承转合、核心矛盾、高潮爆发等）。如果字数极大（如超越 100 万字），请重点细化前中期，后期做宏观走向即可。

请直接输出 markdown 格式的全局大纲，排版要清晰、美观。不要输出多余的客套话。
  `.trim(),
};

export function mergePromptTemplates(partial?: Partial<PromptTemplates>): PromptTemplates {
  return {
    ...DEFAULT_PROMPT_TEMPLATES,
    ...(partial || {}),
  };
}
